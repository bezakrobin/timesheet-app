/*
 * TODO: Implement the login route logic in this file
 *
 * Steps:
 * 1. Import the `express` library.
 * 2. Import the relevant controller function (e.g., `authController.login`) from the controllers directory.
 * - Ensure the path to the controller is correct.
 * 3. Create an instance of the `express.Router()`.
 * 4. Define the `POST /` route:
 * - This route should handle user authentication (login).
 * - It should call the appropriate controller function (e.g., `authController.login`).
 * - Add comments explaining the route, expected request body (email, password), and potential responses (e.g., access token, refresh token, 2FA status).
 * 5. Export the router instance using `module.exports`.
 *
 * Note: Mount this router under /api/auth/login in your main app.
 */
