'use strict';

const { UserRole, USER_ROLE_VALUES } = require('./userRole.enum');
const { ProjectStatus, PROJECT_STATUS_VALUES } = require('./projectStatus.enum');
const { TaskStatus, TASK_STATUS_VALUES } = require('./taskStatus.enum');
const { TaskPriority, TASK_PRIORITY_VALUES } = require('./taskPriority.enum');
const { ProjectRole, PROJECT_ROLE_VALUES } = require('./projectRole.enum');

module.exports = {
    UserRole,
    USER_ROLE_VALUES,
    ProjectStatus,
    PROJECT_STATUS_VALUES,
    TaskStatus,
    TASK_STATUS_VALUES,
    TaskPriority,
    TASK_PRIORITY_VALUES,
    ProjectRole,
    PROJECT_ROLE_VALUES,
};